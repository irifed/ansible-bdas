# Variables for all hosts

iface: eth0

master_ip: "{{ hostvars[groups[master_tag][0]]['ansible_' + iface]['ipv4']['address'] }}"
master_fqdn: "{{ hostvars[groups[master_tag][0]]['ansible_fqdn'] }}"
master_hostname: "{{ hostvars[groups[master_tag][0]]['ansible_hostname'] }}"
master_home: /home/ubuntu

clusteruser: "hadoop"
clusteruser_home: "/home/hadoop"

bdas_container: "ampcamp-data"

# spark and mesos roles depend on this
tachyon_root: "/opt/tachyon"

# integration test requires this
hdfsuser: "hdfs"

master_tag: tag_role_master
worker_tag: tag_role_worker